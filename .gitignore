__pycache__/
*.pyc
.venv/
.sandbox.lock/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Upstream Learning Commons exports (881MB) live OUTSIDE this repo, in
# ~/.cache/teach-upstream/ -- see teach/upstream_provenance.py. This line is
# belt-and-braces for the most likely accidental placement.
teach/data/*.jsonl
